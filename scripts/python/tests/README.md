The `comapare_planar_geographic.py` file was written assuming that 
the version of `raster_chunks` used is the one present in branch `origin/old_stable` of this repository.

Make the appropriate changes to the file, when using it, to account for the current `raster_chunks` module.


